\documentclass[a4paper, 10pt]{article}

\usepackage{hyperref}
\usepackage{xurl}
\usepackage{minted}
\usepackage{graphicx}
\graphicspath{ {./docassets} }
\usepackage{float} % add `H` floating parameter so figures appear like I want
\usepackage{tcolorbox}

% reduce space between images and captions
\usepackage{caption}
\captionsetup{skip=8pt, belowskip=-8pt}

\setminted{
  % linenos,
  % breaklines,
  % frame=lines,
  % framesep=2mm
}

% requirements for work formatting
% 2.5cm edge for pages
\usepackage[margin=2.5cm]{geometry}
% use Helvetica as default font (unfortunately no required Verdana)
\usepackage[T1]{fontenc}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}
% line spacing of 1.5
\usepackage{setspace}
\onehalfspacing
% paragraph spacing of 12 points
\setlength{\parskip}{12pt}
\setlength{\parindent}{0pt} % remove indent at the start of paragraph
% place page numbers on the right
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % clear existing header/footer
\fancyfoot[R]{\thepage} % put page number on the right
\renewcommand{\headrulewidth}{0pt} % remove header line

% format sections by the work format requirements
\usepackage{titlesec}
% section (14pt, uppercase, page break before, 12pt spacing)
\titleformat{\section}
  {\clearpage\bfseries\fontsize{14pt}{16pt}\selectfont}
  {\thesection}{1em}{\MakeUppercase}
\titlespacing*{\section}{0pt}{12pt}{12pt}
% subsction (12pt, uppercase, 12pt spacing)
\titleformat{\subsection}
  {\bfseries\fontsize{12pt}{14pt}\selectfont}
  {\thesubsection}{1em}{\MakeUppercase}
\titlespacing*{\subsection}{0pt}{12pt}{12pt}
% subsubsection (10pt, uppercase, 12pt spacing)
\titleformat{\subsubsection}
  {\bfseries\fontsize{10pt}{12pt}\selectfont}
  {\thesubsubsection}{1em}{\MakeUppercase}
\titlespacing*{\subsubsection}{0pt}{12pt}{12pt}


\title{petope - a VPN development project}
\author{Retro}
\date{2026}

\begin{document}

\maketitle

% Title page does not have numbering
\thispagestyle{empty}



\clearpage
\tableofcontents

% Page numbering starts from second page
\setcounter{page}{1}


\section*{Introduction}
\addcontentsline{toc}{section}{Introduction}

When someone asks how can they connect to their server at home,
for example on \href{https://www.reddit.com/r/selfhosted/}{r/selfhosted} subreddit,
I often see that \href{https://tailscale.com/}{Tailscale} is suggested.

Why you may ask? Well to connect to your own server is not as easy task as
to connect normal public facing server.
Most of home networks are behind NAT with periodically changing IPv4 address
\href{https://en.wikipedia.org/wiki/IPv4_address_exhaustion}{since we don't have enough IPv4 addresses},
and there are firewalls that prevent access from outside.

In some cases you don't even have public IPv4 address,
since ISP provided may use \href{https://en.wikipedia.org/wiki/Carrier-grade_NAT}{CGNAT}
which makes connection from the internet impossible even if you use \href{https://en.wikipedia.org/wiki/Dynamic_DNS}{Dynamic DNS}
and open ports on your router.

And most of these problems are solved by Tailscale!
When you start a Tailscale application, it connects to a \href{https://tailscale.com/docs/concepts/control-data-planes}{control plane}
which coordinates your Tailscale network (known as \href{https://tailscale.com/docs/concepts/tailnet}{tailnet}),
tells you which devices are in it, their internal addresses, public key, manages access to each device
and helps you with \href{https://tailscale.com/blog/how-nat-traversal-works}{NAT traversal}
so you can even connect directly to your server.
Instead of classic VPN structure where you connect to relay server to access internal subnet
you connect to each of your servers directly, thus cutting a middle man and an additional latency
and single point of failure\footnote{there still a single point of failure, as tailnet is managed by control plane instead of relay server}.
And since all clients in the network connect to each other directly,
it is called a \href{https://en.wikipedia.org/wiki/Mesh_networking}{Mesh VPN}.

So, with Tailscale you just connect your device to your tailnet, and you can just to your other devices by an address.
No need to add peers to each device, open ports, manage routing. Plug-n-play solution that just works.

Also it makes a great solution when you want to play with your friends together,
but there is much simpler to use alternative named \href{https://www.radmin-vpn.com/}{Radmin VPN},
which has somewhat similiar functionality, but it is proprietary which means individual user can't be sure
how the program works and if it contains malicious code.

So, after being amazed by how Tailscale works internally, I want to learn by
creating my own VPN solution, learn from my mistakes and while developing everything needed,
and maybe even create something for gamers around the world to use to play together.
And I am totally okay if I won't be able to handle such project, I am sure I will learn a lot of stuff!

\section{Researching existing solutions} \label{existingsolutions}

Since I like \href{https://en.wikipedia.org/wiki/Self-hosting_(network)}{self hosting}
different stuff on my servers, I also like to have control of my data,
particulary don't give a third-party control to it.
That's why I don't fully enjoy Tailscale and Radmin VPN solutions:
\begin{itemize}
  \item Radmin VPN is fully proprietary, so they control the server- and client-side.
        There is nothing stopping them from adding collecting data or even stealing from my PC,
        or adding unauthorized device to my network.
  \item Tailscale has open-source client, but control-plane is fully proprietary and controls
        how client operates. In case of its compomise, your connected devices are fully open
        unless you have configured a firewall. But I trust Tailscale company much more,
        and it feels like a fair trade of security and convinience.
\end{itemize}

But projects like \href{https://syncthing.net/}{Syncthing} and \href{https://yggdrasil-network.github.io/}{Yggdrasil}
inspire me to create something more decentralized, where each device in control of to whom to trust,
and don't rely on single server which commands to whom he can connect and how.

I really liked Yggdrasil approach to the network -
each client is also a server, and an IPv6 address of a client also represents his public key.
This way when you send a packet to an IPv6 address, you can already encrypt message
by using the address, and only client who has right private key can decrypt it.
It is very cool idea since without a single authority you can be sure
that only correct recipient can decrypt and see your messages, and other peers
in the network relay the message until it gets to the right recipient.
Decentralization power, hell yeah!

But without a central server there is no way to find a way how to connect to a peer on the internet.
\href{https://en.wikipedia.org/wiki/BitTorrent}{BitTorrent}
in \href{https://torrentfreak.com/bittorrents-dht-turns-10-years-old-150607/}{old days (before 2005)}
used \href{https://en.wikipedia.org/wiki/BitTorrent_tracker}{trackers}
to find other peers for a torrent you want to download.
The problem is that \emph{distributed file-sharing} relies on \emph{a central server}.
\footnote{which could be improved by using multiple trackers, but it is still not as \emph{distributed}}
To overcome this problem, nowadays \href{https://en.wikipedia.org/wiki/Comparison_of_BitTorrent_clients}{BitTorrent clients}
use \href{https://en.wikipedia.org/wiki/Kademlia}{Kademlia}-based \href{https://en.wikipedia.org/wiki/Distributed_hash_table}{distributed hash table},
aka \href{https://en.wikipedia.org/wiki/Mainline_DHT}{Mainline DHT}.
Basically each peer becomes a distributed tracker, and you stop relying on a single server to find other peers.
This way even if all trackers become offline, you still will be able to find other peers on the internet.
I recommend watching \href{https://www.youtube.com/watch?v=1QdKhNpsj8M}{Kadmelia, Explained} for more in-depth explanation.

\href{https://radicle.xyz/}{Radicle} is another project that aims to bring
p2p to \href{https://en.wikipedia.org/wiki/Git}{Git version control}
which by itself is distributed, but since it is cumbersome to colaborate
in decentralized way, people often use centralized \href{https://en.wikipedia.org/wiki/Forge_(software)}{servers or \emph{forges}}
like \href{https://github.com/}{GitHub} and \href{https://about.gitlab.com/}{GitLab}.
Radicle uses its own protocol named \href{https://app.radicle.xyz/nodes/iris.radicle.xyz/rad%3Az3gqcJUoA1n9HaHKufZs5FCSGazv5}{Heartwood}
which uses a \href{https://radicle.xyz/guides/protocol}{Gossip protocol} to discover other peers and repositories.
For example, imagine this connection between peers:

$$ A \leftrightarrow B \leftrightarrow C \leftrightarrow D $$

So, peer A wants to connect to peer D, but does not know its address.
Peer A \emph{gossips} to peer B, peer B gossips to peer C, and finally peer C returns
response back to peer A. This example shows linear propagation, but in reality a gossip request
propagates through entire network simultaneosly through multiple peers, and each peer stores unique ID
of a gossip request to ignore it next time to prevent a DDoS. This way even without having a direct contact
with peer D, peer A can request information from it.

But also after researching watching the \href{https://www.youtube.com/watch?v=1QdKhNpsj8M}{Kadmelia, Explained} video
I also have started watching other videos from channel \href{https://www.youtube.com/channel/UCdWMU1dBMmB4SrMokGI4jvA}{number 0},
and found out their video \href{https://www.youtube.com/watch?v=RwAt36Xe3UI}{introducing iroh},
a library written in \href{https://rust-lang.org/}{Rust} that does P2P connection between peers effortlessly,
and it just works. After researching this library, it points almost all checks for me, and I would totally use it
for another project that would require such type of networking, or maybe even if it doesn't, just for fun ;)

\section{Planning} \label{planning}

First of all, I am certain that I won't be fully developing \emph{my own} protocol,
since it would require to do extensive work on it.
I plan on using UDP since it would be easier to do \href{https://en.wikipedia.org/wiki/UDP_hole_punching}{NAT hole punching},
but at the same time I would be required to work on
\href{https://www.reddit.com/r/networking/comments/a8c42z/}{delivery reliability}
and \href{https://en.wikipedia.org/wiki/Network_congestion#Congestion_control}{rate controlling},
which I am certain I won't be able to do in such small time window for my diploma work.

\subsection{Core functionality} \label{corefunctionality}

After you first launch the app, a crypto key-pair is generated.
Public key is then used as your identifier.
So, to connect both peers together, you just put respective identifier to each peer,
they automatically discover each other and connect. You don't have to put an endpoint to connect,
everything will be handled automatically as it works in iroh.

About IP addresses, if nothing is specified, then they would be cryptographically generated
from identity, otherwise as specified in config.

Also connection failures will be handled on background, if connection to a peer can't be made,
then constant retries will be done. This also applies to network changes, like Wireguard does it,
if an IP of one peer changes, then another peer must send packets to a new destination.

\subsection{Core choices}
For programming language I choose Rust, since it is used by iroh which I plan using
for my final application.

As for protocol, I will be using UDP and then QUIC, since I believe for the start it would
be easier to deal with more barebones protocol. But I want to try NAT hole-punching with tcp too,
to see if it is even posssible.

About cryptography, I choose \href{https://en.wikipedia.org/wiki/EdDSA#Ed25519}{ed25519}
since I see it is widely used in my ssh keys, Wireguard and iroh too. For symmetric cryptography
I would use \href{ChaCha20}{https://en.wikipedia.org/wiki/ChaCha20-Poly1305} since after researching
\href{https://www.reddit.com/r/crypto/comments/f7c2nv/chacha20_v_aes256/}{Reddit},
\href{https://crypto.stackexchange.com/questions/101050/xchacha20-vs-aes-128-security-and-speed}{stackexchange}
and \href{https://nordpass.com/features/xchacha20-encryption/}{NordPass}
I can say that ChaCha20 would be better general choice.
For hashing \href{https://en.wikipedia.org/wiki/BLAKE_(hash_function)}{blake3}
and \href{https://en.wikipedia.org/wiki/SHA-2}{SHA2} sound like a sane choice.

And that's all I think for now, now it is finally time to do some programming!


\section{Simple UDP server/client}

As my starting point I choose to look into \href{https://doc.rust-lang.org/std/net/struct.UdpSocket.html}{Rust documentation about udp socket},
and wrote following code:

\begin{minted}{rust}
fn server() {
    let socket = UdpSocket::bind("127.0.0.1:3333").unwrap();

    let mut buf = [0; 32];
    let (amt, src) = socket.recv_from(&mut buf).unwrap();

    let buf = &mut buf[..amt];

    println!(
        "received {} from {}",
        str::from_utf8(buf).unwrap_or("raw bytes"),
        src
    );

    buf.reverse();
    socket.send_to(buf, &src).unwrap();
}

fn client() {
    let socket = UdpSocket::bind("127.0.0.1:3334").unwrap();
    socket.connect("127.0.0.1:3333").unwrap();
    socket.send("hello world".as_bytes()).unwrap();

    let mut buf = [0; 32];
    let (amt, src) = socket.recv_from(&mut buf).unwrap();

    let buf = &mut buf[..amt];

    println!(
        "received {} from {}",
        str::from_utf8(buf).unwrap_or("raw bytes"),
        src
    );
}
\end{minted}

Aand I get this after running both server and the client:

\begin{center}
  \includegraphics[width=\textwidth]{1/server.png}
  \includegraphics[width=\textwidth]{1/client.png}
\end{center}

As you can see, I got a message sent from a client to the server,
and server replied with reversed message.
Now it is time to do some discovery!

\section{Peer discovery}

Now that we can connect via UDP, we need to replace an IP address with an identificator.
For this to work, some sort of a discovery server needs to be in place, which
will tell what IP addresses an identifactor has.

\subsection{Discovery server}

My own implementation will use same UDP protocol,
and messages will consist of simple text strings in format of:
\texttt{command|arguments}.

When a discovery server receives a command, it also receives a source address,
this way after receiving \texttt{register|node id} command discovery server
can save source address like: \mintinline{js}|nodes["node id"] = "source address"|
in a \href{https://en.wikipedia.org/wiki/Hash_table}{hash map}.

\subsection{A node/peer}

A node will just have to register itself on the discovery server, and then retrieve an
address of a node it want to connect to via its node id.
Put this algorithm into a loop, which registers itself, finds target address and sends a message to it.

\subsection{Result}

I wont add a source code to this document,
so you can see an implementation of discovery server on commit
\href{https://github.com/dankmolot/petope/tree/5a66b32a015f395bc614a1aedc54a87a44d8dbc1}{5a66b3}.

Since right now discovery uses simple text udp protocol,
I can test it via \href{https://en.wikipedia.org/wiki/Netcat}{netcat} terminal utility:
\begin{center}
  \includegraphics[width=\textwidth]{2/netcat_discovery_example.png}

  And here are discovery logs:

  \includegraphics[width=\textwidth]{2/discovery_server_logs.png}
\end{center}

Great! Now I only need to run both nodes, and wait before they connect to each other.

\begin{center}
  \includegraphics[width=\textwidth]{2/node_alpha.png}
  \includegraphics[width=\textwidth]{2/node_beta.png}
\end{center}

Here you can see that node \texttt{beta} has reached node \texttt{alpha},
and node \texttt{alpha} responded back. Both nodes have found adresses
only via node identificators \texttt{alpha} and \texttt{beta}. Awesome!

Since current implementation constantly tries to find other node and connect to it,
it is ready for UDP hole punching.

\section{UDP hole punching}

To test UDP hole punching capability, I just need to run a \emph{discovery} server on a
dedicated public server, and then run two nodes behing two different networks.

First node will be my notebook connected to my home network,
second one my phone with \href{https://termux.dev/en/}{Termux} installed and
connected to Telia mobile network.
Discovery server will be ran on my rented virtual server provided by Hetzner with static public IPv4 address.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{3/node_on_a_pc.png}
  \caption{successful reply from the phone to the notebook}
  \label{fig:udp_punch_pc}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{3/node_on_a_phone.png}
  \caption{successful incoming message from the notebook to the phone}
  \label{fig:udp_punch_phone}
\end{figure}

As you can see from the screenshots \ref{fig:udp_punch_pc} and \ref{fig:udp_punch_phone}
connected to a discovery server with address \\ \emph{91.107.213.38}
\footnote{my rented server with public address},
notebook with address \emph{88.196.179.131} and phone with address \emph{46.131.76.184}.

Both nodes told discovery server about themselves, retrieved addresses of each other
and since they both sent a packet to each other, NAT routers started allowing incoming packets.
First phone got the message, and afterwards the notebook received
a reply from the phone.

\subsection{About failures}

At first I have tried connecting both nodes from the same network,
my home Wi-Fi, but both nodes weren't receiving incoming packets.
Probably there is some sort of limitation on the router itself, and
it couldn't decide how to forward packets. So for local network there
must be some other way to find each other.

Luckily there is \href{https://en.wikipedia.org/wiki/Zero-configuration_networking}{Zeroconf}
set of technologies that will allow to broadcast and discover services in local network,
so nodes in the same network can connect to each other even without a discovery server.

\href{https://docs.iroh.computer/connecting/local-discovery}{Iroh}
actually uses mDNS-like system to discover endpoints, not enabled by default
but could be enabled easily.

Also another problem I forsee is that both nodes in \emph{different} networks but under \emph{same} CGNAT
\footnote{aka with same public IPv4 address, but with different internal addresses}
will have also a problem connecting to each other, since discovery server
can't know internal CGNAT address of both nodes.

\section{TCP hole punching}

\href{https://en.wikipedia.org/wiki/TCP_hole_punching}{TCP hole punching} on the paper works
similarly to the UDP hole punching, we just need to send a \texttt{SYN} request from both ends
to the nodes addresses, and next packets should go through the NAT.

The problem is implementing the connection via code, most standard libraries only allow
to TCP socket operate in "connect" or "listen" mode, and "connect" mode only allows to
send packets to \emph{one} destination address.

Luckily, there is a \href{https://crates.io/crates/socket2}{socket2} Rust library
which have OS-dependent API calls to create TCP sockets which could be reused for multiple
destination addresses with same local port, \emph{in theory}. Let's experiment!

\subsection{Trying to reuse same socket}
If you are going to try to create two different \href{https://en.wikipedia.org/wiki/Network_socket}{sockets}
with the same local address, operating system will give a "Address already in use" error.
Thankfully most modern operating systems have an option called \texttt{SO\_REUSEPORT} for a socket
which will allow to have multiple sockets on the same local address. This way we can connect
to a discovery server, register our node id, and then try to connect to another node by reusing
same local address and port.

Simple example of using \href{https://crates.io/crates/socket2}{socket2} library:
\begin{minted}{rust}
use std::io;
use socket2::{Domain, Protocol, Socket, Type};

// Creates a TCP socket with SO_REUSEPORT
fn create_socket() -> io::Result<Socket> {
    let socket = Socket::new(Domain::IPV4, Type::STREAM, Protocol::TCP.into())?;
    socket.set_reuse_port(true)?; // sets SO_REUSEPORT for the socket
    Ok(socket)
}

fn main() {
    // First connect to a discovery server, so OS will give us a local addr
    let first_socket = create_socket().unwrap();
    let addr = "127.0.0.1:4444".parse().into(); // <-- discovery server
    first_socket.connect(&addr.into()).unwrap(); // establish a connection with discovery server

    let local_addr = first_socket.local_addr().unwrap()
    println!("local addr: {}", local_addr); // will print out 127.0.0.1:<random port>

    let second_socket = create_socket().unwrap();
    second_socket.bind(local_addr).unwrap(); // reuse same ip:port as in first socket
    second_socket.listen(128).unwrap(); // run socket in listen mode
}
\end{minted}

Without \mintinline{rust}|Socket::set_reuse_port(true)|, this code would fail with an
"Address already in use" error, which you can try to reproduce by changing \texttt{true} to \texttt{false}.

\subsection{First attempt}
Rewriting discovery server to TCP was a pretty simple task, I was forced to open a new thread for
each new connection so it won't be closed immediately, but nothing hard.

Actual hard part was to rewrite a node code, since I can't now carelessly send packets there and
try to receive some, now I need to establish a connection first. But I won't be able to do it since
there is nothing to connect to. So first node tries to connect to another node, or then goes into a
\emph{listen} mode which just waits to receive a message.

First attempt under commit \href{https://github.com/dankmolot/petope/tree/3e83b3}{3e83b3} was
successful in localhost (both nodes in the same \href{https://en.wikipedia.org/wiki/Loopback}{loopback} interface),
but running it on different machines went unsuccessful. I suppose I would need to do a connection + listen
simualtounesly, which currently with synchronous Rust code is hard.

It is time for \href{https://tokio.rs/}{Tokio} library, which brings asynchronous runtime
which in turn will allow me to "easily"\footnote{asynchronous Rust is pretty hard by itself,
  which is why I have avoided it until now.} run both connection attempts and listen for incoming messages.

\subsection{Second attempt}
This time I have went through the \href{https://tokio.rs/tokio/tutorial}{Tokio tutorial}
where they have showed examples based on building an application based on \href{https://github.com/tokio-rs/mini-redis/}{mini-redis}
library which was a great example of how to build a communication. So I have built a basic
\href{https://en.wikipedia.org/wiki/Frame_(networking)}{"framing"} which allowed me to use
simple enums to handle input and output data with ease.

Both discovery server and node code was rewritten to use asynchronous Tokio TCP sockets,
and the main thing that was changed is that now during the start a TCP listener works
on a background, a node registers itself only during the start, and then repeadetly
tries to make a connection to another node. And suprsingly this time it has worked!

So less talking and more showing. Setup is the same, one node on my notebook with a home WiFi,
and another one is my phone connected to a mobile network.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{4/node_notebook.png}
  \caption{notebook searches for the phone and then successfully connects to it}
  \label{fig:tcp_punch_notebook}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{4/node_phone.png}
  \caption{and the phone successfully connects to the notebook from the first try}
  \label{fig:tcp_punch_phone}
\end{figure}

As you can see they have both connected to each other (interestingly enough not through TCP listener),
and sent a message to each other in style \texttt{"yo, i am <node name>"}. I am somewhat certain that I could
archieve similar results with \emph{standard library + socket2} library, but I still learned good practices
from reading the Tokio tutorial and trying to replicate it with my code.
You can try the code on commit \href{https://github.com/dankmolot/petope/tree/c46a8e7b36c998d946180e0317b63daccaaf1a2a}{c46a8e}.

\subsection{About the same NAT}

Nodes that are behind the same NAT still can't connect to each other what is expected. The problem
is that nodes try to connect to each other via \emph{public IP address}, but when router sees that
public address he does not know how to route packets next and just \emph{ignores them}.
Feature \href{https://en.wikipedia.org/wiki/Network_address_translation#NAT_hairpinning}{NAT hairpinning}
or \emph{NAT loopback} was designed to combat with this problem, and my router
\href{https://www.asus.com/support/faq/1043024/}{is in the list of supported routers} so it should work in theory.
In practice, it does not the way I expect. In the \href{https://www.snbforums.com/threads/asus-ac86u-firmware-merlin-386-11-nat-loopback-not-working-please-help.85974/}{forum post}
user \texttt{ColinTaylor} has said that NAT loopback only works if you use port forwarding which I try to avoid.

But the problem leads us to another section!

\section{Multicast DNS}

Instead of trying to discover a node on the internet, we also could try to use a form of \href{https://en.wikipedia.org/wiki/Service_discovery}{Service discovery},
to be precise \href{https://en.wikipedia.org/wiki/Multicast_DNS}{Multicast DNS} which will allow a node to broadcast
itself on the local network, and other nodes to discover and connect. This way even without an internet nodes
still will be able to discover each other and successfully connect.

\begin{tcolorbox}
  Unfortunately this won't help with nodes in different networks but behind one CGNAT,
  unless an ISP provider implements a NAT hairpinning. IPv6 solves this btw.
\end{tcolorbox}

There is a \href{https://crates.io/crates/mdns-sd}{\texttt{mdns-sd}} Rust library which will help
me implement the discovery via the code, all I need is to use it instead of a discovery server.

\subsection{Actual implementation}

It was pretty easy to implement the mDNS integration, since API is pretty straightforward
just like discovery client I have made previously. Here is how my service
\footnote{service is a record that contains a service type, my IP addresses, hostname, some identificator and properties}
is created and registered:

\begin{minted}{rust}
let sd = ServiceDaemon::new()?; // Daemon from mdns-sd
let hostname = utils::get_hostname()?; // returns current hostname with `.local.` appended
let service = ServiceInfo::new(
    "_petope._tcp.local.", // consists of a service name, protocol, and domain (.local)
    &args.id, // identificator, derived from arguments, i.e. "notebook"
    &hostname, // device hostname that must end with .local.
    (), // ip addresses, we use .enable_addr_auto() so the library will add addresses for us
    local_addr.port(), // this is a port of the listener socket, so other node will know to which port to connect to
    None, // properties, we don't use any
)?
.enable_addr_auto();
sd.register(service)?; // ServiceDaemon will handle automatic broadcasting of the service
\end{minted}

Discovery of the service is also very simple:

\begin{minted}{rust}
// fullname consists in form of identification + service type
let node_fullname = format!("{}.{}", node_id, "_petope._tcp.local.");
let receiver = sd.browse("_petope._tcp.local.")?;

loop {
    // ServiceDaemon will automatically search and resolve services for us
    let event = receiver.recv_async().await?;
    match event {
        // we just need to listen for resolved events
        ServiceEvent::ServiceResolved(svc) => {
            // Make sure that service has right identification
            if svc.get_fullname() == node_fullname.as_str() {
                let addresses = svc.get_addresses_v4();
                println!("discovered {} with addresses {:?}", node_id, addresses);
                sd.stop_browse("_petope._tcp.local.")?;

                // create SocketAddr that consists from first ipv4 address + service port
                return Ok(SocketAddr::V4(SocketAddrV4::new(
                    addresses.iter().next().unwrap().to_owned(),
                    svc.get_port(),
                )));
            }
        }
        // ignore all other events
        _ => {}
    };
}
\end{minted}

Function above will wait indefinetely until it will discover the node, return the address
to which the code will connect afterwards. One important thing is that a service name in the service type
\\ (\textbf{\_name}.\_protocol.domain) can't be longer than 15 symbols.

To my suprise it works pretty well, I expected that it would take seconds before a node could discover another node,
but it all takes a fraction of a second. You can see the entire code at commit \href{https://github.com/dankmolot/petope/tree/0915f06ad2b44db91ace9968bc6654252927c25b}{0915f0}.

\section{Developing final application}
After trying some tricks to make p2p connection between two peers, I wanted to implement an
\href{https://en.wikipedia.org/wiki/Minimum_viable_product}{MVP}
with iroh library which I could even use on my servers or to play with my friends.

It will consist of
\begin{enumerate}
  \item Configuration (section~\ref{sec:appconfig}) - to persist private key with identity, and add peers
  \item TUN device (section~\ref{sec:apptun}) - to receive and send IP packets from host
  \item Router (section~\ref{sec:approuter}) - to route packets to different peers depending on the IP address
  \item Peers (section~\ref{sec:apppeers}) - connected via \texttt{iroh} library to receive and send routed IP packets
\end{enumerate}

I actually wrote entire application first before writing this part, so if you want to try it first,
go ahead to commit \href{https://github.com/dankmolot/petope/tree/161ffc0ae3fbb76fd88bc4d2ecf82507cd16596b}{161ffc}.

And also I have made a small diagram that shows how IP packets are routed in the application.
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{5/network_pipeline.png}
  \label{pipeline of how IP packets are routed between systems}
\end{figure}

\subsection{Configuration} \label{sec:appconfig}

Since our \href{https://en.wikipedia.org/wiki/Public-key_cryptography}{pair of keys used for cryptography} is also
acts as identity (Public key is used to connect and to discover a peer), it needs to be persisted somewhere
so on each launch of the application we would have the same identificator, and able to write down identificators
of other peers and able to connect to them.

I choose to store both private key and other peer ids in one single configuration file just for sake of "simplicity"
\footnote{Actually I probably made more complex implementation than if I just used different file for private key.}.
Also I want so if there is no private key, application would generate it and save in the config file automatically.

Since I want not only to read configuration, but also modify it, I would want to not to override any existing comments inside,
and since I choose \href{https://toml.io/en/}{Tom's Obvious Minimal Language aka \emph{TOML}}, I have found a Rust library
\href{https://crates.io/crates/toml_edit}{\texttt{toml\_edit}} which preserves original structure of document while allowing to modify it.

First, I need to be able to specify which config file to use if I want to test application locally. Thankfully it is pretty easy to do with \texttt{clap}.

\begin{figure}[H]
  \begin{minted}{rust}
use clap::Parser;

#[derive(Parser, Debug)]
#[command(version, about, long_about = None)]
struct Cli {
    /// Path to a config file
    #[arg(short, long, default_value_t = String::from("config.toml"))]
    config: String,
}

fn main() {
  let cli = Cli::parse();
}
  \end{minted}
  \caption{example of argument parsing for config file argument}
\end{figure}

This way I can specify either \texttt{-c} or \texttt{--config} argument, i.e. {\texttt{-c config2.toml}} to load a config from
a different file and by default it uses \texttt{config.toml} as initial value.
Thanks to \texttt{clap} I also get help page which will tell about this argument and errors are automatically handled. Neat!


Second, here is a function which either parses \href{https://en.wikipedia.org/wiki/Base64}{Base64} encoded private key
from the config file, or generates a new via
\href{https://docs.rs/iroh/latest/iroh/struct.SecretKey.html#method.generate}{\texttt{SecretKey::generate()}}
and saves final configuration file.
\begin{figure}[H]
  \begin{minted}{rust}
fn get_or_generate_secret_key(path: &str, doc: &mut DocumentMut) -> Result<SecretKey> {
    // get a global "private_key" value from parsed file
    if let Some(encoded) = doc.get("private_key").and_then(|v| v.as_str()) {
        // try to parse string from base64 into bytes
        let decoded =
            utils::base64_decode(encoded).context("private key must be encoded in base64")?;

        // convert to &[u8] and use TryInto which is implemented in SecretKey
        decoded
            .as_slice()
            .try_into()
            .context("private key must be valid ed25519 key bytes encoded in base64")
    } else {
        // No value, no problem, just generate a new
        let key = SecretKey::generate();

        // insert base64 encoded bytes of private key into document
        doc.insert(
            "private_key",
            utils::base64_encode(&key.clone().to_bytes()).into(),
        );

        // and write back to the original file
        std::fs::write(path, doc.to_string())
            .with_context(|| format!("write {} with generated private key", path))?;

        Ok(key)
    }
}
  \end{minted}
  \caption{function that parses private key or generates and saves it}
\end{figure}

Since there is no point of storing private key in the configuration file
\footnote{Private key is only used during Endpoint creation, there is no need to store it longer than it is used for},
other configuration options (like peers) are parsed separately from private key like this:
\begin{figure}[H]
  \begin{minted}{rust}
use iroh::{EndpointId, SecretKey};
use toml_edit::{DocumentMut, Item, Table};

#[derive(Debug, Clone)]
pub struct Config {
    pub peers: Vec<Peer>,
}

#[derive(Debug, Clone)]
pub struct Peer {
    pub id: EndpointId,
}

impl Config {
    fn parse(doc: DocumentMut) -> Result<Self> {
        // iterate over array of tables which looks like this in toml
        // [[peer]]
        // ...
        //
        // [[peer]]
        // ...
        let mut peers = Vec::new();
        if let Some(arr) = doc.get("peer").and_then(Item::as_array_of_tables) {
            for (i, t) in arr.iter().enumerate() {
                peers.push(
                    // here is parsing of each individual peers happens
                    Peer::parse(t).with_context(|| format!("parse a [[peer]] at index {}", i))?,
                );
            }
        }

        Ok(Config { peers })
    }
}

impl Peer {
    fn parse(t: &Table) -> Result<Self> {
        // gets "id" key as a string
        let id = t
            .get("id")
            .and_then(Item::as_str)
            .ok_or_else(|| anyhow!("expected an `id` but got nothing"))?;

        // Use alias to PublicKey::from_z32 to parse z32 string to public key
        let id = EndpointId::from_z32(id).context("parse `id` as an EndpointId")?;

        Ok(Peer { id })
    }
}

  \end{minted}
  \caption{parsing fuctions of peers configuration}
\end{figure}

And then combine \texttt{Config::parse} and \texttt{get\_or\_generate\_secret\_key} functions into one, and load configuration in main:
\begin{figure}[H]
  \begin{minted}{rust}
impl Config {
    pub fn load(path: &str) -> Result<(SecretKey, Config)> {
        // just std::fs::read_to_string(path) which returns an empty string if file
        let data = Config::read_file(path).with_context(|| format!("read config from {}", path))?;

        // Parse into DocumentMut since we modify it in Config::get_or_generate_secret_key
        let mut doc = data
            .parse::<DocumentMut>()
            .with_context(|| format!("parse {}", path))?;

        let private_key =
            Config::get_or_generate_secret_key(path, &mut doc).context("get private key")?;

        // Return private key and config seperately
        Ok((private_key, Config::parse(doc)?))
    }
}

fn main() -> Result<()> {
    // ...
    let (secret_key, config) = Config::load(&cli.config).context("load config")?;
    // secret_key can be later used in Endpoint::builder(...).secret_key(secret_key)
}
  \end{minted}
  \caption{final load function which parses both private key and remaining configuration}
\end{figure}

This way application knows it own private key and public keys (endpoint ids) of other peers.
Implementation could be simpler but this way I retain flexibility of doing fancy stuff with configuration in future.

\subsection{TUN device} \label{sec:apptun}
\href{https://en.wikipedia.org/wiki/TUN/TAP}{TUN device} is a \href{https://en.wikipedia.org/wiki/Network_virtualization}{virtual network device}
which allows a software to receive and send \href{https://en.wikipedia.org/wiki/Internet_Protocol}{IP packets}. As far I know all VPNs are using a TUN devices
\footnote{some rare ones are using a TAP device which allows to access MAC packets, which is mostly useless for regular use-cases.},
and I'll use it since I want to have an IP address derived from a peer endpoint id, so I could connect to them via any software.

As library I chose \href{https://crates.io/crates/tun-rs/}{\texttt{tun-rs}} since it supports all desktop operating systems.

First a device needs to be created, and for that \texttt{tun-rs} provides a very simple interface
\begin{figure}[H]
  \begin{minted}{rust}
use tun_rs::{AsyncDevice, DeviceBuilder};

fn create_device() -> std::io::Result<AsyncDevice> {
    DeviceBuilder::new()
        .name("petope0") // device name that is shown in ifconfig
        .mtu(65535) // MTU, usually 1500, minimum 1280 for ipv6 on Linux
        .ipv4("10.0.0.1", 32, None)
        .ipv6("fdaa::beaf", 128)
        .layer(tun_rs::Layer::L3) // specify L3 for TUN device, L2 for TAP
        .build_async()
}
  \end{minted}
  \caption{example of creating a TUN device with \texttt{tun-rs}}
  \label{code:create_tun}
\end{figure}

Then to receive and send IP packets, library has a very useful abstraction on top of
\href{https://docs.rs/tun-rs/2.8.3/tun_rs/struct.AsyncDevice.html}{AsyncDevice}
called \href{https://docs.rs/tun-rs/2.8.3/tun_rs/async_framed/index.html}{async\_framed}
which allows to use high-level asynchronous API to read and write packets instead of manually
working with \emph{bytes}.
\begin{figure}[H]
  \begin{minted}{rust}
use tun_rs::async_framed::DeviceFramed;
use futures::{SinkExt, StreamExt};

// it is important to wrap AsyncDevice in Arc so it could be splitted
let dev = Arc::new(create_device().unwrap());
// create DeviceFramed and split into send and receive parts
let (mut rx, mut tx) = DeviceFramed::new(dev, BytesCodec::new()).split();

// task that receives IP packets
tokio::spawn(async move {
    // use futures::StreamExt interface to receive
    while let Some(frame) = rx.next().await {
        match frame {
            Ok(bytes) => {
                // process incoming IP packet from "bytes"
            }
            Err(e) => eprintln!("parse frame from tun: {:?}", e),
        }
    }
});

// task that sends packets
tokio::spawn(async move {
    // receive a packet from some sort of channel
    if let Err(e) = tx.send(bytes).await {
        eprintln!("parse bytes to frame to tun: {:?}", e);
    }
});
  \end{minted}
  \caption{example of creating a TUN device with \texttt{tun-rs}}
\end{figure}

As you can see code is pretty simple, and integrates neatly with asynchronous code which is great!

\subsubsection{About huge MTU} \label{sec:hugemtu}
As you have seen in example code \ref{code:create_tun} I have specified a \emph{huge MTU 65535}, but why?

After reading \href{https://yggdrasil-network.github.io/2018/07/13/about-mtu.html}{Yggdrasil blog post}
about their default MTU size of 65535, they explained about \href{https://en.wikipedia.org/wiki/Path_MTU_Discovery}{Path MTU Discovery}
technique which allows to tell the system that "Hey! That packet is too big, please fragment it for $x$ MTU".
Potentially in future iterations huge MTU can allow to optimize network pipeline even more since less fragmention
means less data needs to be sent and less CPU usage for fragmenting will be needed.

\subsubsection{Adding more routes}
Currently TUN device only has specified local IP addresses, but operating system should know that IP addresses of our peers
(see section~\ref{sec:apppeers}) should be routed through our TUN device, otherwise application wouldn't be able to receive packets destined to peers.

And for this I found \href{https://crates.io/crates/net-route}{\texttt{net-route}} library which can add routes
to TUN interface and is cross-platform. Here is an example of adding an IPv4 address routed to our device:
\begin{figure}[H]
  \begin{minted}{rust}
use net_route::{Handle, Route};

// handle is used to manipulate system routing table
let handle = net_route::Handle::new().unwrap();
// interface index is used to associate routes with our TUN device
let ifindex = dev.if_index().unwrap();

// ip address and prefix
let route = Route::new("10.0.0.2".parse().unwrap(), 32)
    .with_ifindex(ifindex);

handle.add(&route).await.unwrap(); // add route above to the routing table
  \end{minted}
  \caption{example of adding a route with \texttt{net-route} library}
\end{figure}

Afterwards all packets will go to the TUN interface and application will be able to process them.

\subsection{Router} \label{sec:approuter}

First a router needs to receive and send IP packets, program need to move bytes somehow.
\href{https://en.wikipedia.org/wiki/Channel_(programming)}{Channels} are popular way to pass data between threads,
and Tokio provides \href{https://docs.rs/tokio/latest/tokio/sync/mpsc/index.html}{\texttt{tokio::sync::mpsc}}
module, but the main problem is that old queued data is persisted until it is processed.
It means that \texttt{mpsc} prioritizes old data instead of new, which is not great
for latency related workloads like a \href{https://en.wikipedia.org/wiki/Real-time_communication}{real-time communcation}.

Instead I found \href{https://github.com/brunocodutra/ring-channel}{\texttt{ring-channel}} library which
implements a channel \href{https://en.wikipedia.org/wiki/Circular_buffer}{ring buffer} which main
feature is \href{https://en.wikipedia.org/wiki/Non-blocking_algorithm}{lock-free} buffer manipulation
and \emph{when buffer is overflown oldest data in queue is replaced with newest}.
Last feature allows to discard old packets when system is overloaded without any hassle.

Also bytes from TUN device.. well.. are just \emph{bytes}. So I use \href{https://crates.io/crates/etherparse}{\texttt{etherparse}}
library to parse raw bytes. Here how it is used in router:
\begin{figure}[H]
  \begin{minted}{rust}
fn route(
    // bytes received from TUN device
    bytes: Bytes,
    // ring-channel buffer destined to TUN device
    to_network_tx: &RingSender<Bytes>
) -> Result<()> {
    // parse bytes into a IP packet from etherparse library
    let ip = IpSlice::from_slice(&bytes[..]).context("parse incoming ip packet")?;
    // IpAddr from where packet has come
    let src = ip.source_addr();
    // IpAddr where packet is destined
    let dst = ip.destination_addr();

    // this is local packet, just return it back to kernel
    if src == dst {
        let _ = to_network_tx.send(bytes);
        return Ok(());
    }

    // TODO: send packets to different peers based on IP address
    
    Ok(())
}
  \end{minted}
  \caption{route function which parses raw bytes into IP packet for IP address based routing}
\end{figure}

Already with this code I can use TUN device to process local packets, there is no need for some complex logic.
Thanks to existing Rust ecosystem, I don't have to learn deeply how IP packets are consisted
and manually do tidious work, instead I can use straightforward library that does hard work for me.

\subsection{Peers} \label{sec:apppeers}

Peer part of the application is responsible to manage a connection, send and receive data.
Router will be responsible to forward packets from TUN device to individual peers depending on destination address.

\subsubsection{Converting peer ID to an IP address}

I liked the idea of Yggdrasil that their IPv6 addresses represent public key of the node, so why not do the same?
\begin{figure}[H]
  \begin{minted}{rust}
// converts EndpointId into an ipv4 address in 10.0.0.0/8 subnet
fn ipv4_from_id(id: &EndpointId) -> Ipv4Addr {
    Ipv4Addr::new(10, id[0], id[1], id[2])
}

// joins two u8 integers into one u16 integer
fn u8_pair(a: u8, b: u8) -> u16 {
    ((a as u16) << 8) | b as u16
}

// converts EndpointId into an ipv6 address in fd22::/16 subnet
fn ipv6_from_id(id: &EndpointId) -> Ipv6Addr {
    Ipv6Addr::new(
        0xfd22,
        u8_pair(id[0], id[1]),
        u8_pair(id[2], id[3]),
        u8_pair(id[4], id[5]),
        u8_pair(id[6], id[7]),
        u8_pair(id[8], id[9]),
        u8_pair(id[10], id[11]),
        u8_pair(id[12], id[13]),
    )
}
  \end{minted}
  \caption{functions that convert EndpointId (public key) into IP addresses}
\end{figure}

With these functions application can generate persistent IP addresses both for \emph{TUN interface} and
\emph{peers}, and they'll be the same on both ends since these IP addresses are converted from public key.

\subsubsection{Routing packets to peers}

Then it is pretty trivial to implement routing from Router to individual peers based on an IP address:
\begin{figure}[H]
  \begin{minted}{rust}
// example of peer structure
struct Peer {
    pub id: EndpointId,
    pub ipv4: Ipv4Addr,
    pub ipv6: Ipv6Addr,
}

impl Peer {
  pub fn new(id: EndpointId) -> Self {
    Peer {
        id,
        ipv4: ipv4_from_id(id),
        ipv6: ipv6_from_id(id),
    }
  }

  pub async fn send(&self, bytes: Bytes) {
    // TODO: connect to the peer and send packet
  }
}

// map of IpAddr to a Peer for routing packets from TUN device
let routes: HashMap<IpAddr, Arc<Peer>> = HashMap::new();
// Example peer
let peer = Arc::new(Peer::new("abcdef...")); // wrapped around Arc<...> so it can be shared

// Add both ipv4 and ipv6 addresses to point to the peer
routes.insert(IpAddr::V4(peer.ipv4), peer.clone());
routes.insert(IpAddr::V6(peer.ipv6), peer.clone());

// Router route function from Router section
fn route(
    bytes: Bytes,
    // ...
) {
    // ip packet parsed here with dst IpAddr
    // ...

    if let Some(target) = routes.get(&dst) {
        target.send().await;
        return;
    }
}
  \end{minted}
  \caption{IP based routing to individual peers}
\end{figure}

\subsubsection{Making a connection with a peer}

But before a packet could be sent, we need to have an estabilished connection.
For this a peer who wants to send data must connect to other peer, and other peer must receive
incoming connection. Initiating connection is pretty simple:
\begin{figure}[H]
  \begin{minted}{rust}
// presets::N0 contain n0 provided relays and discovery services
let endpoint = Endpoint::builder(presets::N0)
    .secret_key(secret_key) // secret key retrived from config previouly
    .alpns(vec![b"petope/0"]) // special string which indicates what protocols are supported
    .bind() // builds Endpoint
    .await
    .unwrap();

// try to connect to the peer by id
let conn = endpoint.connect(peer.id, b"petope/0").await.unwrap()

// here connection can be cached and used to send and receive data
  \end{minted}
  \caption{initiating connection to a peer}
\end{figure}

Receiving a connection is a little bit more complicated, but still pretty easy too:
\begin{figure}[H]
  \begin{minted}{rust}
// additional map to get a peer by its id
let peers: HashMap<EndpointId, Arc<Peer>> = HashMap::new();

// store example peer from previous code
peers.insert(peer.id, peer);

// receive incoming connections until Endpoint was stopped
while let Some(incoming) = endpoint.accept().await {
    // finish handshake and get a connection
    let conn = incoming.await.unwrap() 
    // find related peer and store connection in it
    if let Some(peer) = peers.get(&conn.remote_id()) {
        // TODO: cache connection in peer
    }
}
  \end{minted}
  \caption{receiving incoming connection from another peer}
\end{figure}

Both connection and receiving functions are lacking a function that caches a connection.
Since my code is asynchronous, cached connection could be accessed by multiple
threads simultaneously, and even replaced by a new connection.
I could use \href{https://doc.rust-lang.org/std/sync/struct.Mutex.html}{\texttt{std::sync::Mutex}},
but connection usually would be rarely replaced, but accessed much more frequently.
\href{https://doc.rust-lang.org/std/sync/struct.RwLock.html}{\texttt{std:sync::RwLock}}
looks like a better choice, but there is actually a better alternative.
\href{https://docs.rs/arc-swap/latest/arc_swap/}{\texttt{arc\_swap}} acts similarly
as to \texttt{RwLock}, but is much more cheaper, and updates don't stop entire code
from executing. In reality it won't probably matter, but exploring more options won't hurt.

Anyway, here is missing function that caches a connection:
\begin{figure}[H]
  \begin{minted}{rust}
struct Peer {
    // ...
    // ArcSwapOption is used since connection can be uninitalized
    conn: ArcSwapOption<Connection>
}

impl Peer {
    pub fn set_connection(&self, conn: Arc<Connection>) {
        // replace old value with new connection
        if let Some(old) = self.conn.swap(Some(conn)) {
            // if there was existing connection, close it
            old.close(VarInt::from_u32(0), b"outdated");
        }
    }
}
  \end{minted}
  \caption{function that stores a connection so it could be used later}
\end{figure}

Connection is usually only retrieved when some data needs to be sent, so it could also
automatically try to connect to a peer like this:
\begin{figure}[H]
  \begin{minted}{rust}
impl Peer {
    // get a connection without automatically connecting
    pub fn try_get_connection(&self) -> Option<Arc<Connection>> {
        self.conn
            .load_full()
            // return a connection only if it wasn't closed
            .filter(|conn| conn.close_reason().is_none())
    }

    // gets current non-closed connection or tries to make a connection
    pub async fn get_connection(&self) -> Result<Arc<Connection>, ConnectError> {
        // try to get an existing connection
        if let Some(conn) = self.try_get_connection() {
            return Ok(conn);
        }

        // or try to connect
        match self.endpoint.connect(self.id, ALPN).await {
            Ok(conn) => {
                // connection must be wrapped in Arc due to ArcSwap
                let conn = Arc::new(conn);
                // store connection so it will be reused later
                self.set_connection(conn.clone());
                Ok(conn)
            }
            // just return an error if was unsuccessful
            Err(e) => Err(e),
        }
    }
}
  \end{minted}
  \caption{function that either retrieves existing connection or makes a new one}
\end{figure}

\subsubsection{Telling system to fragment a packet}

Almost everything done, last parts are sending IP packets and receiving them from a peer.
If you remember, TUN device has MTU size of \emph{65535} which is \emph{too big} for typical
MTU of 1500 bytes. That's where Path MTU Discovery comes into the play mentioned in section~\ref{sec:hugemtu},
iroh connection provides a minimal available MTU which could be used to tell operating system to which size
packets should be fragmented via ICMP packets. If you have used a \texttt{ping} command, you are familiar
that ICMP could be used to ping a machine, and ICMP packets could be blocked to hide a machine on the network.
Blocking all ICMP packets is actually harmful to the network and should be avoided, instead only
\href{https://support.huawei.com/enterprise/en/doc/EDOC1100174721/9dff3e87/icmp-echo-request-reply-message}{echo request/reply}
messages should be blocked if you want to silence them.

As specified in \href{https://datatracker.ietf.org/doc/html/rfc4443#section-3.2}{RFC 4443} IPv6 ICMP has a
\emph{Packet Too Big} (type 2, code 0) message which indicates that sent packet was too big and sender must reduce packet size
to given MTU. Also ICMPv6 messages like Packet Too Big must include original IP packet of size that fits minimum MTU.
\begin{figure}[H]
  \begin{minted}{rust}
// returns an icmp response that fragmentation is needed, payload must be the same as ip slice
pub fn fragmentation_needed_response(ip: &IpSlice, payload: &[u8], mtu: usize) -> BytesMut {
    match ip {
        IpSlice::Ipv4(v4) => {} // very similar to ipv6 version
        IpSlice::Ipv6(v6) => {
            let header = v6.header();
            // convert given mtu in u32, or use maximum u32 integer
            let mtu = mtu.try_into().unwrap_or(std::u32::MAX);
            // create a packet destined back to source with PacketTooBig message
            let builder = PacketBuilder::ipv6(header.destination(), header.source(), HOP_LIMIT)
                .icmpv6(Icmpv6Type::PacketTooBig { mtu });

            // cut original IP packet so it will fit into MTU
            let payload_len = payload.len().min(mtu as usize - header.slice().len()); // ipv6 requires entire ip payload in mtu
            let payload = &payload[..payload_len];

            // write icmp packet into a buffer and return it
            let mut writer = BytesMut::with_capacity(builder.size(payload.len())).writer();
            builder.write(&mut writer, payload).unwrap();
            writer.into_inner()
        }
    }
}
  \end{minted}
  \caption{function that creates ICMP message when packet is too big}
\end{figure}

\subsubsection{Sending a packet to a peer}

And now that we have all required functions for sending data, let's combine them:
\begin{figure}[H]
  \begin{minted}{rust}
impl Peer {
    fn send(&self, bytes: Bytes) -> Result<(), SendDatagramError> {
        match self.get_connection() {
            Ok(conn) => {
                // check first if packet fits into MTU
                if let Some(max) = conn.max_datagram_size() {
                    // if not, notify sender to reduce packet size
                    if bytes.len() > max {
                        self.handle_too_big(bytes, max);
                        return Ok(());
                    }
                }

                // simply send bytes to the connection
                conn.send_datagram(bytes)
            }
            Err(e) => {
              eprintln!("unable to connect to {}: {:?}", self.id, e);
              Ok(())
            }
        }
    }

    fn handle_too_big(&self, bytes: Bytes, mtu: usize) {
        // parse bytes into an ip slice
        let ip = IpSlice::from_slice(&bytes[..]).unwrap();
        // create an icmp response based on original packet
        let response = fragmentation_needed_response(&ip, &bytes[..], mtu);
        // send response to the TUN interface
        let _ = self.to_network_tx.send(response.freeze());
    }
}
  \end{minted}
  \caption{complete send function for peer}
\end{figure}

Receiving datagrams is very simple, just calls of\texttt{conn.read\_datagram()} in a loop and
using \\ \texttt{to\_network\_tx.send(bytes)} to forward incoming packets to TUN device.

And that's it, after connecting all these parts from configuration to peers we get
fully ready P2P VPN solution where each peer is locally configured.

\section{Testing how it works}

Because application was written in Rust language, it needs to be compiled first.
On their \href{https://rust-lang.org/learn/get-started/}{"Get Started" page} there is a quick guide
how to install \href{https://en.wikipedia.org/wiki/Software_development_kit}{Rust SDK} on your system.

Then running a command \texttt{"cargo build"} will compile the application to the path \\ \texttt{target/debug/petope},
which could be used to run the application from a command console. Also application requires administrative priveleges
since it creates and uses a TUN device, on Linux you'll need to use a \texttt{sudo} command, like \texttt{sudo ./target/debug/petope}.

After launching application first time, it will create a \texttt{config.toml} file and output information in a screenshot above:
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{5/first_run.jpg}
  \caption{a screenshot which shows current id, IP addresses and empty peers list}
\end{figure}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{5/first_run_config.jpg}
  \caption{created config.toml file contents after running the application}
\end{figure}

As you can see the application tells your id and IP addresses. Inside newly created config file there isn't a lot,
only a private key field so the identity will stay consistent between application starts.
Let's run application on my second computer to get its identifier and make a connection later:

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{5/first_run_server.jpg}
  \caption{a screenshot of started application on second computer}
\end{figure}

Now that I know IDs and IP addresses of both peers, I can add these IDs into config file like this:
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{5/config_with_peer.jpg}
  \caption{config.toml that now contains the peer of the second computer}
\end{figure}

And do the same thing on the second computer, but use peer ID of first computer of course.
Now after running the application second time you can see that now it has a \emph{peer} in the list of peers
with IP addresses that also match IP addresses that were shown on second computer.
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{5/second_run.jpg}
  \caption{second run of the application with a peer of the second computer}
\end{figure}

Now while application runs I can connect to the second computer via IP address that was provided by the application,
and the application will figure out how to connect to the other peer, and cache the connection.
After first IP packet destined to the peer, application will discover the peer by its ID, estabilish initial connection
via a \href{https://docs.iroh.computer/concepts/relays}{relay} and then communicate with the peer to estabilish
a \href{https://en.wikipedia.org/wiki/Peer-to-peer}{peer-to-peer} connection to communicate directly.
You can see this behaviour if you try pinging the peer:
\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{5/pinging.jpg}
  \caption{pinging second computer via both IPv4 and IPv6 addresses provided by the application}
\end{figure}

As you can see first packet took \emph{405ms}, but next took only \emph{4ms}. This means that under a one second
application could discover second peer and connect to it directly in a moment, and all without any additional help
like \href{https://en.wikipedia.org/wiki/Port_forwarding}{port forwarding}
\footnote{iroh library actually uses protocols like \href{https://en.wikipedia.org/wiki/Universal_Plug_and_Play}{UPnP}
  and \href{https://en.wikipedia.org/wiki/NAT_Port_Mapping_Protocol}{NAT-PMP} to open ports on a router
  without specifying any rules beforehand.}.

\subsection{Trying something more serious than pinging}

I have tried playing \href{https://en.wikipedia.org/wiki/Minecraft}{Minecraft} game with my friend who lives
in another country, but at first we couldn't connect to each other, but pings were going through.

The problem was that I have incorrectly implemented ICMP \texttt{Packet Too Big} message, it must include
a part of original IP packet so system can figure out what connection needs fragmentation. After adding
original IP packets as a payload to the messages we had absolutely no problem playing together like
if we were in the same local network.


\section*{Summary}
\addcontentsline{toc}{section}{Summary}

During this I was attaining valuable knowledge of how different techniques work to establish direct connections
between two computers (or peer-to-peer) even behind closed to the internet networks like home and office networks.
Thanks to this work I researched a field about which previously I had very basic understanding and now I am
more confident in building similar systems.

After the research I have experimented with core techniques of establishing direct connection between two computers
on the internet that are unaccesible under normal circumstances, while using a custom made solution to be able
to discover how connection could be made using an arbitrary identifier.

After successful experiments with core techniques, I have fully developed an application can be used
as a VPN solution which establishes direct connections to defined peers by using \texttt{iroh} library.
Library provides an identifier that represent a public key used for encryption and also used to dynamically
discover provided peers on the internet and find paths of possible connections to the peer.

In the end the developed application works under most circumstances, robust and easy to understand due to clear and simple code.
I am certain that my work is already proves to be useful even in such basic state.
In the future I plan to extend and improve the application for my personal use and to play together with my friends in games.

\section*{Summary (võõrnekeelne)}
\addcontentsline{toc}{section}{Summary (võõrnekeelne)}

This work is about "petope - a VPN development project" where Retro was learning and developing his own
VPN solution that is based on peer-to-peer techniques to estabilish direct connection between computers.

Main motivation for the work was a product Tailscale which uses peer-to-peer techniques and provides
user-friendly configuration of the network. Main goal of the work was to learn how peer-to-peer techniques
work under the hood and experiment with them, while in the end creating an application which will be
able to connect to other peers on the internet and create a virtual private network from these peers.

During the work Retro researched existing techniques and applications that are relevant to the topic.
After research Retro has implemented a "Discovery server" which associates an IP address with port
to an arbirtary identifier which later could be used to get IP address with port, and
developed a program which connects directly via TCP and UDP to another computer with technique "hole-punching".
Also Retro has tried using Multicast DNS network protocol as an alternative to the "Discovery server" to find
a peer in the local network.

After these experiments Retro has developed a fully functional VPN application which uses a library "iroh"
to make direct connections similar to Tailscale. Each computer has an indivual identifier which could be retrieved
after launching application first time, and is derived from a private key which is also used for encryption.
In a configuration file after specifying other peers identifiers, while the application is running an user
is able to communicate with other peer via uniquely generated IP addresses while the application maintains
a connection to peer and rouing of IP packets.

In the end Retro has attained a valuable knowledge and developed an useful application.


\section*{Sources}
\addcontentsline{toc}{section}{Sources}
\begin{itemize}
  \item \url{https://yggdrasil-network.github.io/implementation.html}
  \item \url{https://yggdrasil-network.github.io/2018/08/19/congestion-control.html}
  \item \url{https://yggdrasil-network.github.io/2018/07/13/about-mtu.html}
  \item \url{https://yggdrasil-network.github.io/2023/10/22/upcoming-v05-release.html}
\end{itemize}


\end{document}
